<%- div_for parser do -%>
  <h3><%= link_to "Parser details", parser_path(parser) %><%= parser.description.blank? ? '' : " (#{parser.description})" %></h3>
  <p><%= link_to "edit", edit_parser_path(parser) %></p>
  <p>
    <strong>Portal system</strong> <%= parser.portal_system ? link_for(parser.portal_system) : 'None' %><br />
    <strong>Result model</strong> <%= parser.result_model %><br />
    <%- if parser.scraper_type == "ItemScraper" -%>
      <strong>Related model</strong> <%= parser.related_model %><br />
    <%- end -%>
    <strong>Path</strong> <%= parser.path %><br />
    <strong>Cookie Path</strong> <%= parser.cookie_path %>
  </p>
  <h4>Item parser <em>finds items from scraped page</em></h4>
  <code class="item_parser"><%= h parser.item_parser %></code>
  <h4>Item attribute parser <em>extracts each item's attributes from item</em></h4>
	<%= render :partial => 'parsers/attributes', :object => parser.attribute_parser %>
  <br class="clear-left" />
<%- end -%>
